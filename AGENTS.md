# shadow-agent

Repo scaffolded by the `setup-matt-pocock-skills` skill. Configuration for the
engineering skills lives under `docs/agents/`.

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues on `mohmaedeslam00116/shadow-agent` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: root `CONTEXT.md` + ADRs in `docs/adr/`. See `docs/agents/domain.md`.
